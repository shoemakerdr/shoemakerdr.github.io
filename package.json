{
  "name": "derek-shoemaker-portfolio",
  "description": "Derek Shoemaker Portfolio",
  "version": "1.0.0",
  "author": "Derek Shoemaker",
  "homepage": "https://shoemakerdr.github.io",
  "dependencies": {
    "gatsby": "^1.9.23",
    "gatsby-link": "^1.6.5",
    "gatsby-plugin-react-helmet": "^1.0.1"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "deploy": "gatsby build && gh-pages -d build",
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "prettier": "^1.5.2"
  }
}
